package jraft;

public interface CertifierService {
    /*
    void getTimestamp(final boolean readOnlySafe, final CertifierClosure<Long> closure);
    void commit(final BitWriteSet bws, final MonotonicTimestamp monotonicTimestamp, final CertifierClosure<Long> closure);

     */
}